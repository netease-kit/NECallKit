export type ErrorMessage = {
  message: string
}

export const CHANNEL_INFO_NULL: ErrorMessage = {
  message: 'channelInfo is null',
}

export const NOT_CALLING: ErrorMessage = {
  message: 'not calling',
}

export const NOT_BE_CALL: ErrorMessage = {
  message: 'not be call',
}

export const CALL_BUSY: ErrorMessage = {
  message: 'be call busy',
}

export const NOT_IN_CALL: ErrorMessage = {
  message: 'not in call',
}

export const CHANNEL_NOT_MATCH: ErrorMessage = {
  message: 'channel not match',
}
