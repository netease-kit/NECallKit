export type ErrorMessage = {
  message: string
}

export const LOCAL_VIEW_NULL: ErrorMessage = {
  message: 'local view is null',
}

export const LOCAL_STREAM_NULL: ErrorMessage = {
  message: 'local stream is null',
}

export const REMOTE_VIEW_NULL: ErrorMessage = {
  message: 'remote view is null',
}
